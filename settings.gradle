pluginManagement {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://mvnrepository.com/artifact'}

        maven { url 'https://jitpack.io' }
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://mvnrepository.com/artifact'}

        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "MyAgora"
include ':app'
